# 出欠確認アプリのパスワード（必須・両方とも別の値を設定）

# root: CSV アップロード・全員リセットなどの管理操作が可能
ROOT_PASSWORD=change-me-root

# user: 出欠の閲覧・チェックのみ可能
USER_PASSWORD=change-me-user

# Flask セッション署名鍵（任意：未設定なら起動時に自動生成して DB に保存）
# FLASK_SECRET_KEY=
